# 本家ZenzaWatchからの主な変更点

このリポジトリは [segabito/ZenzaWatch](https://github.com/segabito/ZenzaWatch) をもとにした非公式のフォークです。
直接の土台にしているのは、本家を修正した版の `2.6.3-fix-playlist.53` です。
このページでは、元のZenzaWatchとこの版とで**何が違うのか**を、機能ごとにまとめています
（時系列の履歴は [CHANGELOG.md](./CHANGELOG.md) にあります）。

- 各項目の末尾の「（Task NNN）」は、開発の作業番号です。読み飛ばしてかまいません。
- **〔確認中〕** と書いたものは、実装は済んでいますが、実際の環境での確認がまだ終わっていないものです。
  うまく動かない場合は [Issues](https://github.com/ButaMonky/ZenzaWatch/issues) で教えてください。
- 対象のスクリプト: 本体 `ZenzaWatch-dev.user.js`、`ZenzaHLS.user.js`、`MylistPocket.user.js`、
  `ZenzaAdvancedSettings.user.js`、`ZenzaBlogPartsButton.user.js`（それぞれの役割は [SCRIPTS.md](./SCRIPTS.md)）。

---

## 現在のニコニコ動画への対応

- コメントが読み込めなくなっていた問題を修正した（ニコニコ側の仕様変更への対応）。（Task 048）
- 検索APIの移転に対応し、タグ検索・動画検索ができない問題を修正した。（Task 023）
- 検索のたびにエラーが出て失敗することがある問題を修正した。（Task 026）
- 動画情報パネルの「親作品・コンテンツツリー」のリンクを、廃止されたページから現在のページへ差し替えた。（Task 043）
- コンテンツツリーが登録されていない動画でエラーが出ていたのを、「0件」として扱うようにした。（Task 074）〔確認中〕
- サービスが終了した「ニコニコ市場」の表示・処理を削除した。（Task 074）〔確認中〕
- 動画説明文の視聴ページのフルURLや、ショート動画（`ss`〜）のIDを、Zenzaで開けるリンクとして扱うようにした。（Task 074）〔確認中〕
- タグ編集で編集用のキーが期限切れになった時に、自動で取り直して1回だけ再試行するようにした。
  失敗した時に「更新中」のまま固まる問題も修正した。（Task 066）〔確認中〕
- いいね！は、ニコニコ側で成功したことを確かめてから表示を切り替えるようにした。（Task 066）〔確認中〕

## 動画再生 / domand / HLS（ZenzaHLS）

- 高画質で再生できる配信方式（domand / DMC）の判定を、現在のニコニコの応答に合わせて作り直した。（Task 022）
- ZenzaHLSのバグを修正した。エラーの種類が正しく伝わらない、再生情報の保存先の更新に失敗する、
  hls.js の読み込みに失敗した時の代わりの表示が出ない、などの問題。（Task 034）
- 再生を終えた後もアドレスバーが動画のURLのまま残り、ページを再読み込みするとその動画のページが開いてしまう問題を修正した。（Task 046）
- 「音声の自動調整」を追加した（本家と同じく、動画ごとの音量差を小さくする。既定はOFF）。
  今は音量を下げる方向の調整だけで、元より大きくはできない。（Task 074）〔確認中〕

## 自動画質選択

- 画質「自動」なのに、回を重ねるごとに低い画質（360p など）から始まるようになる問題を修正した。
  次回の最初の画質を、実際に測った回線速度から決めるようにした。（Task 079）〔確認中〕
- GPU（グラフィックドライバー）が使えない環境で、60fpsの画質が候補から外れて 360p に固定される問題を修正した。（Task 079b）〔確認中〕
- 画質メニューの「自動」に、自動で選ばれる最大の画質を表示するようにした（例:「自動（最大720p）」）。（Task 079b）〔確認中〕

## コメント機能

- 「コメントを動画の後ろに流す」をONにすると、コメントの位置が大きくずれる問題を修正した。（Task 052）
- コメント入力欄の「コマンド」を、本家のようにサイズ・位置・色を選んで指定できるようにした。
  入力欄にフォーカスすると下に選択肢が並び、詳しいパネルも開ける。（Task 073）〔確認中〕
- Enterキーでコメントが2回送信され得る作りだったのを修正した。（Task 073）〔確認中〕
- 動画の最後の「提供」画面の間に投稿されたコメントも、その画面の間に流れるようにした。（Task 081）〔確認中〕

## 検索

- 検索を、本家の検索ページと同じ仕組みに切り替えた。並び順・件数が本家の検索結果と一致することを確認している。
  投稿者・チャンネル・コメント数で絞り込む時などは、従来の検索方式を使う。（Task 067）〔確認中〕
- 並び順を本家と同じ9種類（＋昇順/降順）にし、投稿期間・再生時間・ジャンルで絞り込めるようにした。（Task 068）〔確認中〕
- 検索欄に、本家と同じタグの候補（検索予測）を表示するようにした。キーボードでも選べる。（Task 069・072・073）〔確認中〕
- 検索欄に動画ID（`sm`〜、視聴ページのURL、`nico.ms` の短縮URL）を入れると、すぐ移動せずに、検索欄の下へ動画のカードを表示するようにした。
  カードから再生・次に再生・プレイリストの最後に追加・とりあえずマイリストができる。（Task 071）〔確認中〕
- 「再生中の動画の投稿者の動画のみ」で絞り込めるようにした（以前は取り消し線付きで使えなかった）。（Task 073・074）〔確認中〕
- 検索の読み込みを速くした（2ページ目以降をまとめて取得。1000件で約6秒→約2.4秒）。（Task 074）〔確認中〕
- タグの ▶ ボタンでの検索（タグ検索）で、並び順などが正しく反映されない問題を修正した。（Task 056・058）
- チャンネルでの絞り込みが効かない問題を修正した。（Task 024）

## タグ / ニコニコ大百科

- タグの大百科アイコン（記事の有無）が表示されなくなっていた問題を修正した。（Task 044〜047）
- 再生中の動画のジャンルを、タグ欄の先頭にバッジで表示するようにした。
  押すと、検索欄のジャンルの絞り込みにそのジャンルが入る。（Task 074・075）〔確認中〕
- 大百科の記事内の動画埋め込みから、Zenzaで動画を開けなくなっていた問題を修正した（ZenzaBlogPartsButton）。（Task 028）

## プレイリスト

- 親作品・子作品（コンテンツツリー）の動画を、まとめてプレイリストに読み込めるようにした（親→子の順。件数が多くても全件取得する）。（Task 043・044）
- ユーザーページからの連続再生の後にショート動画の連続再生を読み込むと、プレイリストに動画が入らない問題を修正した。（Task 041・042）〔確認中〕
- 検索でプレイリストに読み込む最大件数を、設定で変えられるようにした（100〜5000件。推奨は1000件まで）。（Task 073）〔確認中〕
- 広告（ニコニ広告）の金冠・銀冠を、プレイリストにも表示できるようにした（設定でON/OFF）。
  多くの動画をまとめて問い合わせる方式で、件数が多くても通信回数を抑えている。（Task 054〜057・066）〔確認中〕
- 表示が消えることがあった検索結果一覧・コメント一覧の不具合を修正した。（Task 025）
- マイリスト・とりあえずマイリスト・ランキングの取得が失敗することがある問題、マイリストの101件目以降が反映されない問題を修正した。（Task 024）

## MylistPocket

- 投稿者の情報が無い動画で、動画情報の読み込みが終わらない問題を修正した。（Task 069）〔確認中〕
- ランキングページで「？」ボタンを押すと消えて、以後出なくなる問題を修正した。（Task 079）〔確認中〕
- 動画情報パネルのタグの「？」の色で、大百科の記事の有無が分かるようにした。（Task 079b）〔確認中〕

## 小画面モード（画面モード「小」）

- 画面左上に固定だった「小」モードを、ドラッグで好きな場所へ動かせるようにした（動画全体をつかんで動かせる。画面の外には出ない）。（Task 029〜033）
- 右下のつまみで大きさを変えられるようにした。縦横比の固定／自由を設定で選べる。（Task 032）
- 位置と大きさを記憶し、初期状態に戻すボタンも付けた。（Task 032）
- 動かしても、右上のメニュー（いいね・マイリスト・閉じる など）やコメント入力欄が動画について来るようにした。
  入力欄は画面の端では上下を反転して表示する。（Task 035〜039・050）
- 一定時間操作しないと下のコントロールバーが透明になり、マウスを乗せると戻るようにした。（Task 051）
- 動かした状態で次の動画を開くと、読み込み中の表示だけが左上に出る問題、
  「小」以外の画面モードでも縦横比の固定ボタンが表示される問題を修正した。（Task 049）
- 「小」モードの位置を初期状態に戻すショートカットを追加した。（Task 060）〔確認中〕

## PiP（ピクチャー・イン・ピクチャー）

- 流れるコメントも一緒に映る「P in P(コメント付き)」を追加した（右クリックメニュー・ショートカット）。
  画面フィルターの効果も反映される。（Task 070・077）〔確認中〕
  - 既知の制限: 別のタブを表示している間に、ブラウザが映像の処理を止めて PiP の映像が止まる可能性がある（確認中）。

## 画面フィルター（エフェクト）

- 動画の見た目を調整する画面フィルターを追加した。明るさ・コントラスト・彩度・色相・ぼかし・ガンマ（暗い動画を見やすくする）・
  黒/白レベル・シャープ・色温度・色かぶり・自動レベル補正・周辺減光・着色 など。（Task 077・077b・077c）〔確認中〕
- プリセットを用意した。コントロールバーの「エフェクト」ボタンから開くパネルで調整でき、
  左右で元の映像と見比べることもできる。（Task 077c・080）〔確認中〕
- 以前からあった左右反転・上下反転を画面フィルターに統合し、一般会員でも右クリックメニューから使えるようにした。（Task 077）〔確認中〕
- スクリーンショットにも画面フィルターを反映できる（設定で選択）。（Task 077）〔確認中〕
- 既知の制限: 「くっきり」（シャープ）など一部の項目は処理が重く、GPUの無い環境ではなめらかに表示できないことがある。

## 動画の最後の「提供」画面

- 本家と同じく、動画の最後にニコニ広告・ギフトの支援者を表示する「提供」画面を再現した（提供音声・ギフトの演出を含む。設定でON/OFF）。（Task 080）〔確認中〕
- 動画を開いた直後の読み込みが遅くならないよう、提供画面の情報は動画の残りが45秒になってから読み込むようにした。（Task 081）〔確認中〕

## シークバー

- シーンに合わせたサムネイル（シークバーのプレビュー画像）を、一般会員でも表示できるようにし、表示までを速くした。（Task 070）〔確認中〕
- シークバーのプレビュー画像の画質が、意図どおりに選ばれない問題を修正した。（Task 020）

## 設定・ショートカット

- ショートカットキーを、上級者向け設定で自由に割り当てられるようにした。キーを押して登録するボタンも付けた。
  操作できる項目を大幅に増やした（関連メニュー、画面フィルター、再生速度13段階 など）。（Task 059〜061・077・080）〔確認中〕
- 秒数を自由に決められる「カスタムシーク」のスロットを10個追加した（以前の、秒数を入力できない予備スロットは廃止）。（Task 065）
- 上級者向け設定に、コメントパネルの表示ON/OFFを追加した。（Task 065）
- 上級者向け設定を点検し、効いていなかった設定（エコノミーモードで強制的に開く、コメントパネル表示ON/OFFのショートカット）を修正し、
  意味の無くなった項目を削除した。（Task 063・064）
- 動画ヘッダー（検索欄・タグ）を「動画の外に出す／重ねて隠す」を、設定で選べるようにした（通常・大モード）。（Task 072・076）〔確認中〕

## UI / 操作性

- 動画ヘッダーを「常に動画の外」にした時に、ヘッダーが動画にかぶさって操作しづらい問題を直した。（Task 076）〔確認中〕
- 検索欄に入力している間や、メニューを開いている間に、動画ヘッダーが消えないようにした。（Task 076）〔確認中〕
- 全画面表示でも、コントロールバーを自動で透明にするようにした。一時停止した時は一度表示する。（Task 053）〔確認中〕
- いいね！のお礼メッセージを、動画情報パネルに残してコピーできるようにした（URLはリンクになる）。（Task 074）〔確認中〕
- 検索予測が出ないことがある問題を修正した。（Task 074）〔確認中〕
- 画面フィルターのパネルは、開閉のアニメーションが付き、パネルの外を押すと閉じるようにした。（Task 080）〔確認中〕

## API・通信まわり

- ニコニコの通信を調べた結果に基づき、通信の失敗や無駄な問い合わせを減らした（広告の冠の一括取得の分割、投稿者情報の補完 など）。（Task 066）〔確認中〕
- シークバーのプレビュー画像の取得を、動画情報の取得直後に始めるようにし、画像は裏でまとめて取得して保存するようにした。（Task 070）〔確認中〕

## 開発・保守面の改善

- テストが最後まで動くようにテスト環境を整え、使われていないファイルを整理した。（Task 001〜019）
- インストールされているスクリプトのバージョンが、Tampermonkeyのダッシュボードとブラウザのコンソールに表示されるようにした。（Task 039・055）
- スクリプトの自動更新先（`@downloadURL`・`@updateURL`）を、このリポジトリに向けた。
  スクリプトの名前・namespace は本家と同じなので、本家の版に上書きする形でインストールされる（設定は引き継がれる）。（Task 078）
- 新しく追加・変更した部分のライセンスを MIT-0 にした。（Task 011・016）

## 既知の制限・未対応

- `ZenzaWatch.user.js`（旧通常版）は古い版のままです。`ZenzaWatch-dev.user.js` を使ってください。両方を同時に有効にしないでください。
- 全画面表示で、シークバーのコメント表示が設定ボタン・全画面ボタンに重なって押しにくいことがあります（未対応）。
- 「前の動画」を開こうとすると、再読み込みだけが起きることがあるという報告があります（調査中）。
- 読み上げ機能（`yomi`）は、元のコードに実装が欠けているため復活させていません。
